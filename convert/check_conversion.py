import subprocess


def get_missing_subjects(subject_dir: str, origin_dir: str) -> list:
    '''
    Compares the extracted subjects to the origin
    Parameters
    ----------
    subject_dir
    origin_dir

    Returns
    -------

    '''